% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_roxygen_all.R, R/build.R
\name{buildFLIsjjm}
\alias{buildFLIsjjm}
\title{Construct FLIndices from a jjm.output object}
\usage{
buildFLIsjjm(out)
}
\arguments{
\item{out}{A one or two-stock, single run, jjm.output object.}

\item{stock}{Numeric. Stock index to process; for single-stock runs this
is 1. Default is 1.}

\item{stock=1}{Stock for which indices are to be loaded.}
}
\value{
An \code{FLIndices} object built from the provided output.

An \code{FLIndices} object.
}
\description{
Transform a single-run jjm.output object into an \code{FLIndices}
collection. This lower-level builder is used internally by
\code{readFLIsjjm} and other wrapper helpers. It maps the jjm output
structure into FLIndex objects including selectivity, index q and time
ranges.

This function creates an \code{FLIndices}, a list of \code{FLIndexBiomass} objects,
each of them containing the data and estimates of a single index of abundance.
Slots on each \code{FLIndexBiomass} object are filled with the following information:
\itemize{
\item index: \code{data$Index}.
\item index.var: \code{data$Indexerr}.
\item catch.n: \code{data$Ipropage}.
\item catch.wt: \code{data$Iwtatage}.
\item effort: Empty.
\item sel.pattern: \code{output@sel_ind_i}, where \code{i} refers to the index number.
\item index.q: \code{output@q_i}, where \code{i} refers to the index number.
\item range 'startf' and 'endf': (\code{data$Imonths} - 1) / 12.
}
}
\details{
The function expects the jjm output to include objects named
\code{info}, \code{data}, \code{output}, \code{control} and
\code{parameters}. It will extract indices, their selectivity patterns
(sel.pattern), catchability (index.q) and transform start and end
months into fractional positions within a year.
}
\examples{
\dontrun{
mod <- jjmR::readJJM("h1_1.07", path = system.file("ext-data","single_stock", package="FLjjm"))
idx <- buildFLIsjjm(mod)
}
idx <- readFLIsjjm("h1_1.07",
  path = system.file("ext-data", "single_stock", package="FLjjm"))
summary(idx)
plot(idx)
}
\seealso{
\code{readFLIsjjm}
}
\author{
Iago Mosqueira \href{mailto:iago.mosqueira@wur.nl}{iago.mosqueira@wur.nl}
}
