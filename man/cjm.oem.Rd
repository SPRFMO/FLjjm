% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_roxygen_all.R
\name{cjm.oem}
\alias{cjm.oem}
\title{Observation Error Model wrapper for CJM within MSE workflows}
\arguments{
\item{stk}{An \code{FLStock} object used as the operating model.}

\item{deviances}{A list or object with deviance specifications for the
OEM sampling routine.}

\item{observations}{List containing observation specifications (e.g.
indices and their sampling properties) used by the OEM.}

\item{stability}{Numeric. Stability parameter for the OEM (default 1).}

\item{wts}{Logical. Whether to use weights (default TRUE).}

\item{jjms}{Logical. Whether to run the jjms-specific OEM branch
(default TRUE).}

\item{F3sel}{(internal) Selection pattern for F3 (passed to the sampler).}

\item{args}{List. Additional arguments controlling dimension settings such
as dy, frq and other temporal helpers used by the OEM wrapper.}

\item{tracking}{Logical or list. Optional tracking and diagnostic settings.}
}
\value{
A list with sampled observation time series and diagnostics as
prepared by the \code{sampling.oem} helper and post-processing steps.
}
\description{
Run the CJM Observation Error Model (OEM) for a given \code{FLStock} and
observation configuration. This function prepares inputs, calls the
sampling.oem helper and post-processes results to match the expected
structure for later MSE steps.
}
\details{
This function is tailored for the CJM/OEM integration inside the
MSE flow used by the package. It sets up the temporal dimension arguments
(using \code{dy} and \code{frq} from \code{args}), calls
\code{sampling.oem} and performs light post-processing such as dropping
data columns not required for downstream routines.
}
\examples{
\dontrun{
res <- cjm.oem(stk, deviances = list(), observations = list(), args = list(dy=2022, frq=1))
}
}
\seealso{
\code{sampling.oem}
}
\author{
Iago Mosqueira \href{mailto:iago.mosqueira@wur.nl}{iago.mosqueira@wur.nl}
}
