% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_roxygen_all.R, R/build.R
\name{buildFLRPsjjm}
\alias{buildFLRPsjjm}
\title{Build reference points (FLPar) from jjm.output}
\usage{
buildFLRPsjjm(out, stock = 1)
}
\arguments{
\item{out}{A jjm.output object (single-run) or a wrapper list typically
returned by \code{jjmR::readJJM}.}

\item{stock}{Numeric. Stock index to select the correct element in the
output list when multiple stocks are present. Default is 1.}

\item{name}{Name of the ctl model file, \emph{character}.}

\item{path}{Path ot the model folder structure, \emph{character}.}

\item{stock=1}{Stock to extract, of relevance on 2 stocks model runs, \emph{numeric}.}
}
\value{
An \code{FLPar} with named reference points: MSY, SB0, SBMSY and
FMSY.

An object of class FLPar.
}
\description{
Create an \code{FLPar} holding reference points such as MSY, SB0, SBMSY
and FMSY using the output slot \code{msy_mt} in the jjm output. Units are
set to match the package conventions (e.g. "1000 t" for biomass-based
quantities and "f" for fishing mortality).

An object of class \emph{FLPar} is created from the information in a jjm.output
object and for a given stock, if more than one is present.
The function currently extracts the following reference points from the
\code{msy_mt} data.frame in the \code{output} element of \code{jjm.output}:
\itemize{
\item MSY: Maximum Sustainable Yield, in thousands of tonnes.
\item SB0: Virgin spawning biomass, in thousands of tonnes.
\item SBmsy: Spawning biomass at MSY, in thousands of tonnes.
\item Fmsy: Fishing mortality at MSY.
}
}
\details{
The function reads the last row of the \code{msy_mt} table in
the output and maps the columns msy, bzero, bmsy and fmsy to the
corresponding FLPar elements. The units are set explicitly in the
returned object.
}
\examples{
\dontrun{
mod <- jjmR::readJJM("h1_1.07", path = system.file("ext-data","single_stock", package="FLjjm"))
rps <- buildFLRPsjjm(mod)
rps
}
rps <- readFLRPsjjm(name = "h1_1.07",
  path = system.file("ext-data", "single_stock", package="FLjjm"))
summary(rps)
}
\seealso{
\code{jjmR::readJJM}

\link[jjmR:readJJM]{jjmR::readJJM} \link[FLCore:FLBiol]{FLCore::FLBiol} buildFLBjjm
}
\author{
Iago Mosqueira \href{mailto:iago.mosqueira@wur.nl}{iago.mosqueira@wur.nl}
}
