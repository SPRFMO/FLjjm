% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_roxygen_all.R
\name{buildFLSjjm}
\alias{buildFLSjjm}
\title{Build FLStock from jjm.output}
\arguments{
\item{out}{A jjm.output object (single-run) or similar list.}

\item{stock}{Numeric. Stock index to extract (default 1).}

\item{name}{Character. Optional name for the resulting FLStock. Default "CJM".}
}
\value{
An \code{FLStock} object constructed from the jjm model output.
}
\description{
Build a standard \code{FLStock} object (without explicit area breakdown)
from jjm.output. The returned object contains landings, stock numbers,
weights, natural mortality, maturity, and other standard FLStock slots.
}
\details{
The function constructs age and year dimnames from the input
metadata (\code{data$age} and \code{data$year}) and fills relevant
slots using the output matrices such as \code{output$N} for
stock.n and \code{output$wt_a_pop} for stock weights. Units are set
according to the package conventions (e.g. "1e6" for counts where
appropriate, or "1000 t" for biomass-based numbers).
}
\examples{
\dontrun{
mod <- jjmR::readJJM("h1_1.07", path = system.file("ext-data","single_stock", package="FLjjm"))
stk <- buildFLSjjm(mod)
}
}
\seealso{
\code{buildFLSojjm}
}
\author{
Iago Mosqueira \href{mailto:iago.mosqueira@wur.nl}{iago.mosqueira@wur.nl}
}
